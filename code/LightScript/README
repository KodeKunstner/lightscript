LightScript is a subscript of EcmaScript,
with a compact implementation running
on low end mobile devices.

./net/lightscript class files and generated Java source files,
this is what is needed when embedding the scripting language.

./src contains the actual source code, from which ./net/lightscript
is generated. Some contains CPP-directives, and is thus not directly
compilable, - use the Makefile

./doc contains documentation, and generated javadoc

./TODO to do list, and roadmap

Standard library:
print(str)
gettype(val)
clone(obj)
Array.prototype.pop()
Array.prototype.join(sep)
parseInt(str, radix)
Array.prototype.push(val)

Object.constructor()
Array.constructor()
Array.concat(..)
Array.prototype.slice(start,end)
Array.prototype.sort(comparefn)
String.prototype.slice(a, b)
String.prototype.charCodeAt(pos)
String.fromCharCode(c);
String.concat(...)
