- static getter-setter interface and extend stdlib
  ::: LightScript thing: APPLY, SET, GET!
    - default object, for non-LightScript things
    - LightScript a LightScript-thing
        - with apply -> undefined, 
        - set,get-> global values!
        - default this.
  - remove length special case from compiler
  - add new, delete
  - use clone, delete, and iterator from getter
  - replace lightscript object with std-lib-functions on Hashtable
- script for automatic wrapper generation
- api
- make fixed point optional
- testsuite
